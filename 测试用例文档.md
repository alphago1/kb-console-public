# KB Console 全功能测试用例文档

> 生成日期：2026-05-03
> 测试工程师：Claude Code
>
> 本文档覆盖 kb_tool CLI 全 32 个命令、Streamlit GUI 全 9 个页面、MCP 全 7 个工具接口的功能测试用例。

---

## 一、已发现的 Bug（阻塞性）

在编写测试用例前，先记录代码审查中发现的已知问题：

### BUG-001: `no such table: documents` — 数据库初始化缺失

**严重级别：P0 阻塞**

**影响范围：** 除 `weekly-organize` 外的几乎所有 workflow_mainline.py 函数

**根因分析：**

| 函数 | 调用 `KBDatabase.init()` | 调用 `_ensure_doc_columns()` | 状态 |
|------|--------------------------|------------------------------|------|
| `weekly_organize()` | ✓ 第174行 | ✓ 第175行 | 正常 |
| `token_budget()` | ✗ 缺失 | ✓ 第340行 | **BUG** |
| `build_trading_bundle()` | ✗ 缺失 | ✓ 第378行 | **BUG** |
| `trading_monthly_report()` | ✗ 缺失 | ✓ 第441行 | **BUG** |
| `trading_system_build()` | ✗ 依赖 bundle | ✗ 间接调用 | **BUG** |
| `trading_analyze()` | ✗ 缺失 | ✗ 缺失 | **BUG** |
| `find_idea()` | ✗ 缺失 | ✗ 缺失 | **BUG** |
| `compact_course_transcripts()` | ✗ 缺失 | ✗ 缺失 | **BUG** |
| `fetch_folder_docs()` (bundle_builder) | ✗ 缺失 | ✓ `_ensure_columns` | **BUG** |
| `fetch_topic_docs()` (bundle_builder) | ✗ 缺失 | ✓ `_ensure_columns` | **BUG** |
| `fetch_profile_docs()` (bundle_builder) | ✗ 缺失 | ✓ `_ensure_columns` | **BUG** |

**修复建议：** 在每个函数的 DB 操作前统一调用 `KBDatabase(cfg).init()` 再调用 `_ensure_doc_columns(sqlite_path)`，或将两者合并为一个 `_init_db(cfg)` 函数。

### BUG-002: 中文字符路径乱码

**严重级别：P1 严重**

**状态：✅ 已确认**

**现象：** 路径中中文显示为乱码（如 `交易系统与方法论` 显示为 `����ϵͳ�뷽����`）

**实测：**
- CLI JSON 输出中所有中文路径、分类名、片段内容全部变成乱码
- 文件系统实际存储正常（UTF-8），但 stdout 输出时编码不匹配

**根因分析（已定位）：**
```
Python filesystem encoding: utf-8    ← 文件系统正确
stdout encoding: gbk                 ← 终端编码为 GBK！
LANG env: NOT SET
PYTHONIOENCODING: NOT SET
```
- Windows 中文版控制台默认代码页为 GBK（CP936）
- Python 检测到 GBK 终端后，将 UTF-8 文件路径按 GBK 编码输出 → 乱码
- GBK 无法编码 Emoji 字符（如 📊🧠），agent_test 中 3/5 测试的 print() 抛出 `UnicodeEncodeError`
- Streamlit GUI 中的 `run_cli_live` 使用 `encoding="utf-8", errors="ignore"` 会静默丢弃 GBK 范围外字符

**修复建议：**
1. CLI 入口处设置 `PYTHONIOENCODING=utf-8` 环境变量，或 `sys.stdout.reconfigure(encoding='utf-8')`
2. Windows 终端执行前先 `chcp 65001` 切换到 UTF-8 代码页
3. JSON 输出统一使用 `json.dump(ensure_ascii=False)` 后写文件而非打印到 stdout
4. Streamlit `run_cli_live` 的 `errors="ignore"` 改为 `errors="replace"` 以便发现丢失字符

### BUG-003: agent_test 错误统计 — 编码错误被误计为 PASS=False

**严重级别：P2**

**状态：✅ 已确认**

**现象：** agent_test 中 3/5 个问题在 print() 时因 GBK 无法编码 Emoji 而抛出 `UnicodeEncodeError`，被 `except Exception` 捕获后误输出 `PASS=False`，但 Agent 调用实际内部返回了有效答案。最终汇总显示 "5/5 passed" 与单条 "PASS=False" 矛盾。

```python
# main.py:456-465 的 bug:
ok = bool((res.get("answer") or "").strip())  # ok=True
if ok:
    passed += 1  # ✓ 计数正确
print(f"[{i}] PASS={ok} ...")  # ✗ print 抛出 UnicodeEncodeError → 跳到 except
# except 里打印 "PASS=False" 但 ok 实际是 True
```

**修复建议：** 1) 先修复 BUG-002（编码）；2) print 失败不应进入工具执行异常分支，用 `sys.stdout.write()` 替代 `print()` 并捕获 `UnicodeEncodeError`。

### BUG-004: search 命令在不存在文件上 extract 报错

**严重级别：P3**

**现象：** `search` 命令（line 118: `build_chunks(cfg, rebuild=False)` 调用 chunker）对所有 DB 中的文档逐个读取时，若文件在索引后被移动/删除，`extract_text` 抛出 `FileNotFoundError`。

```
ERROR extract failed: C:\...\docs\_weekly_inbox\2026-01\# 法律大模型对比实验全量数据报告与论文提案.md
FileNotFoundError: No such file or directory
```

**修复建议：** `extract_text` 调用前检查文件是否存在，缺失文件记录 warning 并跳过。

---

## 二、测试环境准备

### 前置条件
- [ ] `.venv` 虚拟环境存在且依赖完整
- [ ] `kb_tool/config.yaml` 配置文件存在且路径有效
- [ ] `kb_tool/kb_out/kb.sqlite3` 数据库文件存在
- [ ] `docs/` 目录存在且包含分类子目录
- [ ] `DEEPSEEK_API_KEY` 环境变量已设置（LLM 相关测试需要）

### 测试前检查清单
```bash
# 1. 验证 python 环境
.venv\Scripts\python.exe --version

# 2. 验证数据库
.venv\Scripts\python.exe -c "import sqlite3; con=sqlite3.connect('kb_tool/kb_out/kb.sqlite3'); print(con.execute('SELECT COUNT(*) FROM documents').fetchone())"

# 3. 验证配置文件
.venv\Scripts\python.exe -c "import yaml; print(yaml.safe_load(open('kb_tool/config.yaml',encoding='utf-8')))"
```

---

## 三、CLI 命令测试用例（32 个命令）

### 🔴 优先级 1：数据核心命令

| 用例ID | 命令 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|--------|------|----------|----------|----------|------|
| TC-CLI-01 | `scan --dry-run` | `python main.py scan --config config.yaml --dry-run --max-files 5` | 成功执行，打印扫描统计信息，不修改 DB | | 待测 |
| TC-CLI-02 | `scan`（正式） | `python main.py scan --config config.yaml --max-files 10` | 成功执行，提取文本并写入 SQLite，输出 LLM 分类结果 | | 待测 |
| TC-CLI-03 | `scan` 边界：max-files=0 | `python main.py scan --config config.yaml --max-files 0` | 不处理任何文件，正常退出 | | 待测 |
| TC-CLI-04 | `scan` 边界：不存在的 config | `python main.py scan --config nonexistent.yaml` | 输出明确的 FileNotFoundError，非 Python traceback | | 待测 |

---

| 用例ID | 命令 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|--------|------|----------|----------|----------|------|
| TC-CLI-05 | `docs-migrate --dry-run` | `python main.py docs-migrate --config config.yaml --dry-run` | 预览模式，显示将迁移的文件列表，不实际复制 | | 待测 |
| TC-CLI-06 | `docs-migrate`（正式） | `python main.py docs-migrate --config config.yaml` | 将源文件复制到 docs/，更新 sqlite 中路径引用 | | 待测 |
| TC-CLI-07 | `docs-migrate --only-included` | `python main.py docs-migrate --config config.yaml --only-included` | 仅迁移 include_in_kb=1 的文档 | | 待测 |
| TC-CLI-08 | `docs-migrate` 边界：docs 已存在目标文件 | 重复执行 docs-migrate | 不报错，跳过已存在的文件或正确处理覆盖 | | 待测 |

---

| 用例ID | 命令 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|--------|------|----------|----------|----------|------|
| TC-CLI-09 | `docs-stats` | `python main.py docs-stats --config config.yaml` | 生成 `docs_字数统计报告.md`，包含总览、按月统计、按类别×月份统计 | | 待测 |
| TC-CLI-10 | `docs-stats --output` | `python main.py docs-stats --config config.yaml --output test_stats.md` | 输出到指定路径 `test_stats.md` | | 待测 |
| TC-CLI-11 | `docs-stats` 边界：空 docs 目录 | 在空 docs 目录执行 | 优雅报告 0 文件，不报错 | | 待测 |

---

| 用例ID | 命令 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|--------|------|----------|----------|----------|------|
| TC-CLI-12 | `build-chunks` | `python main.py build-chunks --config config.yaml` | 对所有 include_in_kb=1 文档分块并建立 FTS 索引 | | 待测 |
| TC-CLI-13 | `build-chunks --rebuild` | `python main.py build-chunks --config config.yaml --rebuild` | 先删除旧索引再重建 | | 待测 |

---

| 用例ID | 命令 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|--------|------|----------|----------|----------|------|
| TC-CLI-14 | `search` 中文查询 | `python main.py search --config config.yaml --query "止损"` | 返回包含"止损"的 chunk 列表，JSON 格式正确 | | 待测 |
| TC-CLI-15 | `search` 英文查询 | `python main.py search --config config.yaml --query "RAG"` | 返回包含"RAG"的 chunk 列表 | | 待测 |
| TC-CLI-16 | `search` 多词查询 | `python main.py search --config config.yaml --query "止损 执行力"` | 返回同时匹配两个词的片段 | | 待测 |
| TC-CLI-17 | `search --limit` | `python main.py search --config config.yaml --query "交易" --limit 5` | 只返回 5 条结果 | | 待测 |
| TC-CLI-18 | `search` 边界：无匹配 | `python main.py search --config config.yaml --query "xyzabc123不存在"` | 返回空 JSON 数组 `[]` | | 待测 |
| TC-CLI-19 | `search` 边界：空查询 | `python main.py search --config config.yaml --query ""` | 不崩溃，返回空结果或提示需要非空查询 | | 待测 |
| TC-CLI-20 | `search` 边界：无 chunk 表 | 在未 build-chunks 的情况下执行 search | 优雅报错或自动 build-chunks，不崩溃 | | 待测 |

---

### 🟡 优先级 2：报表与导出命令

| 用例ID | 命令 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|--------|------|----------|----------|----------|------|
| TC-CLI-21 | `export` | `python main.py export --config config.yaml` | 导出 `documents.csv` 和 `documents.json` 到 kb_out/exports/ | | 待测 |
| TC-CLI-22 | `export` 边界：空数据库 | 在没有 documents 表或表为空时执行 | 不崩溃，导出空 CSV/JSON 文件 | | 待测 |
| TC-CLI-23 | `dashboard` | `python main.py dashboard --config config.yaml` | 生成 kb_out/dashboard/index.html，可浏览器打开 | | 待测 |
| TC-CLI-24 | `report` | `python main.py report --config config.yaml --quarter 2026-Q2` | 生成季度报告 markdown 文件 | | 待测 |
| TC-CLI-25 | `report` 不带 --quarter | `python main.py report --config config.yaml` | 使用 config 默认季度 | | 待测 |
| TC-CLI-26 | `monthly-report` | `python main.py monthly-report --config config.yaml --month 2026-03` | 生成 2026-03 月报 | | 待测 |
| TC-CLI-27 | `bundle` | `python main.py bundle --config config.yaml` | 生成 model_context_bundle.md | | 待测 |
| TC-CLI-28 | `normalize-tags` | `python main.py normalize-tags --config config.yaml` | 归一化数据库中已有的 emotion/topic 标签 | | 待测 |
| TC-CLI-29 | `review --threshold 0.5` | `python main.py review --config config.yaml --threshold 0.5` | 生成 review.html 和 review_updates.csv | | 待测 |
| TC-CLI-30 | `apply-review` | `python main.py apply-review --config config.yaml --file <csv_path>` | 将人工复核修改写回 SQLite | | 待测 |

---

### 🟢 优先级 3：主工作流命令（workflow_mainline）

| 用例ID | 命令 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|--------|------|----------|----------|----------|------|
| TC-CLI-31 | `weekly-organize --dry-run` | `python main.py weekly-organize --config config.yaml --dry-run --max-files 10` | 扫描但不写入，输出预览 JSON | | 待测 |
| TC-CLI-32 | `weekly-organize`（正式） | `python main.py weekly-organize --config config.yaml --max-files 10` | 正式整理：COPY 文件→分类→写入 docs/→生成周报 | | 待测 |
| TC-CLI-33 | `weekly-organize` 路径：中文文件名 | 源文件包含中文名称 | 正确创建中文目录和文件名，不乱码 | | **重点关注** |
| TC-CLI-34 | `weekly-organize` SQL注入防护 | 文件名包含 `'; DROP TABLE--` | 参数化查询，不触发 SQL 注入 | | 待测 |

---

| 用例ID | 命令 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|--------|------|----------|----------|----------|------|
| TC-CLI-35 | `token-budget trading` | `python main.py token-budget --config config.yaml --scope trading` | 返回交易文档 token 估算 | | **BUG-001** |
| TC-CLI-36 | `token-budget folder` | `python main.py token-budget --config config.yaml --scope folder --folder "AI与工具化"` | 返回指定文件夹 token 估算 | | 待测 |
| TC-CLI-37 | `build-folder-bundle` | `python main.py build-folder-bundle --config config.yaml --folder "AI与工具化"` | 生成文件夹 bundle，不调用 LLM | | 待测 |
| TC-CLI-38 | `analyze-folder` | `python main.py analyze-folder --config config.yaml --folder "AI与工具化" --question "核心研究方向?"` | 调用 LLM 分析文件夹，输出报告 | | 待测 |
| TC-CLI-39 | `project-analyze` | `python main.py project-analyze --config config.yaml --topic "RAG" --question "novelty在哪?"` | 跨文件夹项目分析，输出报告 | | 待测 |
| TC-CLI-40 | `profile-me all` | `python main.py profile-me --config config.yaml --scope all` | 全量个人画像 | | 待测 |
| TC-CLI-41 | `profile-me trading` | `python main.py profile-me --config config.yaml --scope trading` | 交易画像 | | 待测 |
| TC-CLI-42 | `profile-me ai-projects` | `python main.py profile-me --config config.yaml --scope ai-projects` | AI 项目画像 | | 待测 |
| TC-CLI-43 | `build-trading-bundle` | `python main.py build-trading-bundle --config config.yaml` | 生成 trading bundle，不调用 LLM | | **BUG-001** |
| TC-CLI-44 | `trading-monthly-report` | `python main.py trading-monthly-report --config config.yaml --month 2026-03` | 生成交易月报，调用 LLM | | **BUG-001** |
| TC-CLI-45 | `trading-system-build` | `python main.py trading-system-build --config config.yaml` | 生成交易系统构建报告 | | **BUG-001** |
| TC-CLI-46 | `trading-analyze` | `python main.py trading-analyze --config config.yaml --topic "止损"` | 生成止损主题分析报告 | | **BUG-001** |
| TC-CLI-47 | `find` 基础查询 | `python main.py find --config config.yaml --query "不等回调"` | 返回匹配文档列表 | | **BUG-001** |
| TC-CLI-48 | `find` 带过滤 | `python main.py find --config config.yaml --query "止损" --category "交易系统与方法论" --month-start 2026-01 --month-end 2026-03` | 返回过滤后结果 | | **BUG-001** |
| TC-CLI-49 | `compact-course-transcripts` | `python main.py compact-course-transcripts --config config.yaml` | 压缩课程转写内容 | | **BUG-001** |

---

### 🔵 优先级 4：Web 服务与 MCP

| 用例ID | 命令 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|--------|------|----------|----------|----------|------|
| TC-CLI-50 | `serve` 启动 | `python main.py serve --config config.yaml --host 127.0.0.1 --port 8000` | FastAPI 服务在 8000 端口启动 | | 待测 |
| TC-CLI-51 | `serve` 健康检查 | `curl http://127.0.0.1:8000/` | 返回 200，返回内容合理 | | 待测 |
| TC-CLI-52 | `serve` 端口冲突 | 在已占用端口启动 | 给出明确错误信息 | | 待测 |

---

| 用例ID | 命令 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|--------|------|----------|----------|----------|------|
| TC-CLI-53 | `mcp-list-tools` | `python main.py mcp-list-tools --config config.yaml` | 列出所有已启用的 MCP 工具名 | | 待测 |
| TC-CLI-54 | `mcp-smoke-test` | `python main.py mcp-smoke-test --config config.yaml` | 执行冒烟测试：happy path + 权限拒绝 + 路径防护 | | 待测 |
| TC-CLI-55 | `mcp-http` 启动 | `python main.py mcp-http --config config.yaml --host 127.0.0.1 --port 8765` | MCP HTTP 服务启动 | | 待测 |
| TC-CLI-56 | `mcp-stdio` | 通过管道发送 JSON 请求 | 返回正确 MCP 响应 | | 待测 |

---

### ⚫ 优先级 5：Agent

| 用例ID | 命令 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|--------|------|----------|----------|----------|------|
| TC-CLI-57 | `agent` 单问 | `python main.py agent --config config.yaml --query "我这三个月止损执行有什么变化?"` | Agent 执行多轮工具调用，返回回答 | | 待测 |
| TC-CLI-58 | `agent-test` | `python main.py agent-test --config config.yaml` | 执行 5 个预置问题，返回 pass/fail | | 待测 |
| TC-CLI-59 | `agent` 边界：tool-calling 超过 max_steps | 复杂问题触发多步调用 | 在 max_steps=6 步后截断，不无限循环 | | 待测 |
| TC-CLI-60 | `agent` 边界：权限拒绝 | Shell/write 操作 | Agent 拒绝执行，记录审计 | | 待测 |

---

## 四、MCP 工具接口测试用例（7 个工具）

### 前提
- 假设 MCP HTTP 服务运行在 `http://127.0.0.1:8765`

### 工具 1: `kb.search_documents`

| 用例ID | 输入参数 | 预期结果 | 状态 |
|--------|----------|----------|------|
| TC-MCP-01 | `{"query": "止损"}` | 返回最多 10 条匹配文档，包含 docs_path、category、snippet | |
| TC-MCP-02 | `{"query": "止损", "limit": 5}` | 返回最多 5 条 | |
| TC-MCP-03 | `{"query": "止损", "month_start": "2026-01", "month_end": "2026-03"}` | 仅返回该时间范围内的文档 | |
| TC-MCP-04 | `{"query": "止损", "primary_category": "交易系统与方法论"}` | 仅返回该类别的文档 | |
| TC-MCP-05 | `{"query": "C:\\Windows"}` | **被拒绝** → `{"error": "path-like arguments are not allowed"}` | |
| TC-MCP-06 | `{"query": "止损"}` → 检查返回体 | 包含 `disclaimer` 字段；路径被脱敏为 `[KB_ROOT]` | |
| TC-MCP-07 | `{"query": "", "limit": 10}` | 不报错（或给出参数错误） | |
| TC-MCP-08 | 结果数量 > 20 | 被 `_trim_result` 截断到 20 条 | |

### 工具 2: `kb.search_chunks`

| 用例ID | 输入参数 | 预期结果 | 状态 |
|--------|----------|----------|------|
| TC-MCP-09 | `{"query": "背驰"}` | 返回 chunk 级匹配片段，包含 snippet 和 score | |
| TC-MCP-10 | `{"query": "背驰 三买"}` | 返回匹配多个词项的结果 | |
| TC-MCP-11 | `{"query": "/etc/passwd"}` | 路径防护可能触发拒绝（取决于 regex） | |
| TC-MCP-12 | 结果数量 > 30 | 被截断到 30 条 | |

### 工具 3: `kb.get_document`

| 用例ID | 输入参数 | 预期结果 | 状态 |
|--------|----------|----------|------|
| TC-MCP-13 | `{"document_id": 1}` | 返回文档详情：filename, category, content 等 | |
| TC-MCP-14 | `{"document_id": 999999}` | 返回空或 "not found" | |
| TC-MCP-15 | `{"document_id": -1}` | 优雅处理：返回空或错误提示 | |

### 工具 4: `kb.compare_periods`

| 用例ID | 输入参数 | 预期结果 | 状态 |
|--------|----------|----------|------|
| TC-MCP-16 | `{"start_month": "2025-12", "end_month": "2026-03", "topic": "止损"}` | 返回两个时段关于止损的对比分析 | |

### 工具 5: `kb.summarize_month`

| 用例ID | 输入参数 | 预期结果 | 状态 |
|--------|----------|----------|------|
| TC-MCP-17 | `{"month": "2026-03"}` | 返回 2026-03 的分类分布和重点摘要 | |

### 工具 6: `kb.find_writing_candidates`

| 用例ID | 输入参数 | 预期结果 | 状态 |
|--------|----------|----------|------|
| TC-MCP-18 | `{}` | 返回 writing_potential 评分最高的文档列表 | |
| TC-MCP-19 | `{"limit": 5}` | 返回 5 条候选 | |

### 工具 7: `kb.cluster_project_ideas`

| 用例ID | 输入参数 | 预期结果 | 状态 |
|--------|----------|----------|------|
| TC-MCP-20 | `{}` | 返回项目想法聚类结果 | |
| TC-MCP-21 | `{"month_start": "2026-01"}` | 仅返回该月之后的聚类 | |

### MCP 通用安全测试

| 用例ID | 测试场景 | 预期结果 | 状态 |
|--------|----------|----------|------|
| TC-MCP-22 | 调用不存在的工具 `kb.delete_file` | 返回 `{"error": "tool not allowed"}` | |
| TC-MCP-23 | 审计日志存在 | `kb_out/logs/mcp_audit.jsonl` 中有每次调用记录 | |
| TC-MCP-24 | HTTP 非 MCP 请求 | 随机 HTTP 请求返回合理错误 | |

---

## 五、Streamlit GUI 测试用例（9 个页面）

### 前置条件
- Streamlit 应用启动：双击 `run_kb_console.bat` 或通过命令行启动
- GUI 访问 `http://127.0.0.1:8501`

### 页面 1: 首页 Dashboard

| 用例ID | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|--------|----------|----------|----------|------|
| TC-GUI-01 | 页面正常加载 | 打开 GUI，默认进入首页 | 显示 3 个 metric：文件总数、总字数、交易 token 预算 | |
| TC-GUI-02 | metric 数据显示 | 观察 3 个 metric | 数字全部 > 0（如果数据库有数据） | |
| TC-GUI-03 | "刷新状态"按钮 | 点击"刷新状态" | 页面刷新，数据不变或更新 | |
| TC-GUI-04 | "打开 docs 文件夹"按钮 | 点击 | 调用 `os.startfile` 打开文件资源管理器 | |
| TC-GUI-05 | "打开 reports 文件夹"按钮 | 点击 | 同上打开 reports 文件夹 | |
| TC-GUI-06 | 最近报告表格 | 观察表格 | 展示最近 10 个报告文件名和修改时间 | |
| TC-GUI-07 | 最近 weekly report 预览 | 如果有周报 | 在 expander 中渲染 Markdown | |
| TC-GUI-08 | **BUG 验证**: 数据库不存在 | 删除或重命名 kb.sqlite3 后刷新 | **不崩溃**，metrics 显示 0 或 N/A | **重点关注** |
| TC-GUI-09 | **BUG 验证**: token_budget 错误 | 数据库未初始化时进入首页 | trading_token_budget 调用不导致页面崩溃 | **BUG-001** |

### 页面 2: 每周整理 Weekly Organize

| 用例ID | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|--------|----------|----------|----------|------|
| TC-GUI-10 | 页面加载 | 点击左侧"每周整理" | 显示 source_dirs 列表，max_files 输入框（默认 200） | |
| TC-GUI-11 | Dry Run | 设置 max_files=5，点击"Dry Run" | 执行 preview，不写入文件/DB，显示 JSON 结果 | |
| TC-GUI-12 | 正式整理（需勾选确认） | 设置 max_files=5，勾选确认，点击"正式整理" | 执行正式整理，调用 LLM，显示进度日志 | |
| TC-GUI-13 | 不勾选确认直接点正式整理 | 不勾选，点击"正式整理" | 按钮 disabled，无法点击 | |
| TC-GUI-14 | 日志实时输出 | 执行整理时观察日志 | 日志实时滚动，显示处理文件名和行数 | |
| TC-GUI-15 | 执行完成后显示报告 | 整理完成 | 显示"打开文件夹"和"复制路径"按钮 + Markdown 预览 | |

### 页面 3: 找文件 Find

| 用例ID | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|--------|----------|----------|----------|------|
| TC-GUI-16 | 基础搜索 | 输入 query="不等回调"，点击"搜索" | 返回匹配文档列表，含 snippet、category、month | |
| TC-GUI-17 | 按分类过滤 | 选择 category="交易系统与方法论"，搜索 | 仅返回该分类的结果 | |
| TC-GUI-18 | 按月份过滤 | 输入 month="2026-03"，搜索 | 仅返回该月份结果 | |
| TC-GUI-19 | 组合过滤 | category + month + query 同时设置 | 返回同时满足三个条件的结果 | |
| TC-GUI-20 | 空查询 | query 为空，点击"搜索" | 不崩溃，返回空或无结果 | |
| TC-GUI-21 | 打开文件按钮 | 点击某结果的"打开文件" | 调用 `os.startfile` 打开 .docx 文件 | |
| TC-GUI-22 | **BUG 验证** | 数据库不存在时使用查找 | 不崩溃，优雅提示 | **BUG-001** |

### 页面 4: 交易分析 Trading

| 用例ID | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|--------|----------|----------|----------|------|
| TC-GUI-23 | token-budget trading | 点击"1) token-budget trading" | 显示 JSON token 预算结果 | |
| TC-GUI-24 | **BUG 验证**: token-budget | 数据库未初始化 | 不崩溃 | **BUG-001** |
| TC-GUI-25 | 生成 Trading Bundle | 点击"2) 生成 Trading Bundle" | 输出 JSON，包含 bundle 路径，**不调用 LLM** | |
| TC-GUI-26 | Bundle 预览 | Bundle 生成后查看 | 可预览 Markdown 内容 | |
| TC-GUI-27 | trading-monthly-report（需确认） | 输入 month="2026-03"，勾选确认，点击执行 | 调用 LLM 生成交易月报 | |
| TC-GUI-28 | trading-system-build（需确认） | 勾选确认，点击执行 | 调用 LLM 构建交易系统报告 | |
| TC-GUI-29 | trading-analyze（需确认） | 输入 topic="止损执行"，勾选确认，点击执行 | 调用 LLM 生成主题分析报告 | |
| TC-GUI-30 | 安全：不勾选 LLM 确认 | 点击需要 LLM 的按钮但不勾选 | 按钮 disabled | |
| TC-GUI-31 | LLM 调用日志 | 执行 LLM 操作后检查 | `kb_out/logs/llm_calls.jsonl` 有记录 | |

### 页面 5: 项目/文件夹全文分析

| 用例ID | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|--------|----------|----------|----------|------|
| TC-GUI-32 | 文件夹下拉列表 | 进入页面 | 显示 docs 目录下所有非下划线开头的子文件夹 | |
| TC-GUI-33 | token-budget folder | 选择 folder，点击"1) token-budget folder" | 显示该文件夹的 token 估算 JSON | |
| TC-GUI-34 | build-folder-bundle | 选择 folder，点击"2) build-folder-bundle" | 生成 bundle，不调用 LLM，显示文件路径 | |
| TC-GUI-35 | analyze-folder（需确认） | 选择 folder，输入 question，勾选确认 | 调用 LLM 分析，生成报告 | |
| TC-GUI-36 | "仅生成 bundle"模式 | 勾选"仅生成 bundle"，点击 analyze-folder | 即使不勾选 LLM 确认也生成 bundle | |
| TC-GUI-37 | project-analyze（需确认） | 输入 topic + question，勾选确认 | 调用 LLM 跨文件夹分析，生成报告 | |
| TC-GUI-38 | 中文文件夹路径 | 选择包含中文的文件夹名 | 路径不出现乱码 | **BUG-002** |

### 页面 6: 个人画像 Profile

| 用例ID | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|--------|----------|----------|----------|------|
| TC-GUI-39 | scope=all | 选择"all"，勾选确认，点击执行 | 生成全量个人画像报告 | |
| TC-GUI-40 | scope=trading | 选择"trading"，勾选确认，点击执行 | 生成交易画像报告 | |
| TC-GUI-41 | scope=ai-projects | 选择"ai-projects"，勾选确认，点击执行 | 生成 AI 项目画像报告 | |
| TC-GUI-42 | 不勾选确认 | 不勾选，点击执行 | 按钮 disabled | |

### 页面 7: 课程转写压缩

| 用例ID | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|--------|----------|----------|----------|------|
| TC-GUI-43 | 执行压缩 | 勾选确认，点击"执行压缩" | 调用 LLM 压缩所有课程转写文档 | |
| TC-GUI-44 | 显示压缩结果 | 压缩完成 | 显示 JSON 摘要，output_dir 可打开 | |
| TC-GUI-45 | 预览示例压缩稿 | 压缩完成 | 显示第一个压缩文件的 Markdown 预览 | |

### 页面 8: 报告中心 Reports

| 用例ID | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|--------|----------|----------|----------|------|
| TC-GUI-46 | 报告分组列表 | 进入页面 | 按子目录分组显示报告（weekly, trading_monthly 等） | |
| TC-GUI-47 | 浏览报告 | 选择分组 → 选择文件 | 显示预览 Markdown | |
| TC-GUI-48 | 打开文件夹 | 点击"打开文件夹" | 在资源管理器中打开所在目录 | |
| TC-GUI-49 | 复制路径 | 点击"复制路径" | 路径写入系统剪贴板 | |
| TC-GUI-50 | 边界：无报告 | reports 目录为空 | 显示"暂无报告"提示 | |

### 页面 9: 设置 Settings

| 用例ID | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|--------|----------|----------|----------|------|
| TC-GUI-51 | 显示配置 | 进入页面 | 显示 config.yaml 路径、docs_dir、kb_out_dir、source_dirs | |
| TC-GUI-52 | LLM 信息 | 观察 LLM 区域 | 显示 provider/model="deepseek / deepseek-v4-flash" | |
| TC-GUI-53 | API Key 安全 | 观察 API Key 区域 | **仅显示"存在/不存在"，不显示 Key 值** | |
| TC-GUI-54 | 安全策略显示 | 观察底部 | 显示安全约束说明文字 | |
| TC-GUI-55 | 配置中文路径 | 检查路径显示 | 中文路径正确显示，不出现乱码 | **BUG-002** |

---

## 六、跨模块集成测试

| 用例ID | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|--------|----------|----------|----------|------|
| TC-INT-01 | 端到端：scan → search → find | 1) scan 新文件 2) search 索引 3) GUI find 页面搜索 | 数据流通畅，三处结果一致 | |
| TC-INT-02 | 端到端：weekly-organize → Dashboard | 1) weekly-organize 2) 刷新 Dashboard | Dashboard 显示更新后的文件数和字数 | |
| TC-INT-03 | 端到端：analyze-folder → 报告中心 | 1) analyze-folder 2) 报告中心查看 | 报告出现在报告中心和正确目录 | |
| TC-INT-04 | 并发：同时两个 CLI 操作 | 两个终端同时执行 scan | 不出现 SQLite 锁错误 | |
| TC-INT-05 | 中文路径全链路 | 所有操作经过中文路径的文件 | 无乱码 | **BUG-002** |
| TC-INT-06 | 空数据库冷启动 | 全新安装，无 kb.sqlite3 | 所有 DB 操作自动创建表，无 "no such table" | **BUG-001** |
| TC-INT-07 | GUI → CLI 路径一致性 | GUI 调用 CLI 命令 | 子进程工作目录正确，config 路径正确 | |
| TC-INT-08 | windows_task_scheduler 集成 | 执行 `run_weekly_organize.bat` | 正确调用 CLI 命令 | |
| TC-INT-09 | windows_task_scheduler 月报 | 执行 `run_trading_monthly_report.bat 2026-04` | 生成对应月报 | |

---

## 七、性能与边界测试

| 用例ID | 测试场景 | 操作步骤 | 预期结果 | 状态 |
|--------|----------|----------|----------|------|
| TC-PERF-01 | 大文件 scan | 500+ 文件目录扫描 | 不超时，不 OOM | |
| TC-PERF-02 | 大文档 chunking | 2000+ chunks 文档 | FTS 索引正常工作 | |
| TC-PERF-03 | Markdown 预览大报告 | 报告 > 120KB | 被截断到 120000 chars，不卡死 UI | |
| TC-PERF-04 | subprocess 超时 | LLM 调用超时（60s） | 进程不僵尸，GUI 有错误提示 | |
| TC-PERF-05 | 长 path 处理 | Windows 路径 > 260 字符 | 不报 PathTooLong 错误 | |

---

## 八、安全审计测试

| 用例ID | 测试场景 | 预期结果 | 状态 |
|--------|----------|----------|------|
| TC-SEC-01 | 源文件只读 | 所有工具不修改/删除/重命名 docs 中的源文件 | |
| TC-SEC-02 | 输出路径白名单 | 仅写入 kb_out/{reports,bundles,logs} | |
| TC-SEC-03 | 路径遍历攻击 | 请求 `../../../etc/passwd` 被拒绝 | |
| TC-SEC-04 | API Key 不泄露 | 任何界面/日志中不出现 DEEPSEEK_API_KEY 值 | |
| TC-SEC-05 | MCP 审计日志 | 每次 MCP 工具调用都记录到 mcp_audit.jsonl | |
| TC-SEC-06 | Agent 审计日志 | 每次 Agent 工具调用记录到 agent_audit.jsonl | |
| TC-SEC-07 | LLM 调用日志 | 每次 LLM 调用记录 prompt_chars 和 response_chars | |
| TC-SEC-08 | MCP 路径脱敏 | 所有返回路径被替换为 [KB_ROOT] | |
| TC-SEC-09 | MCP 路径输入拒绝 | path-like 参数被拒绝 | |

---

## 九、测试执行计划

### 第一轮：Bug 验证（优先修复阻塞性 Bug）
1. BUG-001: 验证所有 DB 操作在无 sqlite 表时的表现
2. BUG-002: 验证中文路径乱码具体场景

### 第二轮：核心接口测试
1. 执行 10 个核心 CLI 测试用例（TC-CLI-01 ~ TC-CLI-20）
2. 执行 9 个 GUI 首页+Dashboard 测试用例

### 第三轮：全量回归测试
1. 执行所有 55 个 GUI 测试用例
2. 执行所有 60 个 CLI 测试用例
3. 执行所有 24 个 MCP 测试用例
4. 执行所有 9 个集成测试用例

---

## 十、测试数据说明

| 资源 | 路径 | 用途 |
|------|------|------|
| 数据库 | `kb_tool/kb_out/kb.sqlite3` | 200 个文档，970K 字 |
| 文档目录 | `docs/` | 12 个分类 × 12 个月 |
| 报告目录 | `kb_tool/kb_out/reports/` | 历史报告归档 |
| Bundle | `kb_tool/kb_out/bundles/` | 分析 bundle |
| 日志 | `kb_tool/kb_out/logs/` | LLM 调用 + 审计日志 |

---

## 十一、2026-05-03 实际测试报告

### 测试执行摘要

| 项目 | 数值 |
|------|------|
| 测试日期 | 2026-05-03 18:50-19:00 |
| 数据库状态 | documents 表存在，201 条记录，47 列完整 |
| MCP 工具 | 7 个全部列出正确 |

### 实际执行测试结果

| 用例ID | 测试项 | 结果 | 备注 |
|--------|--------|------|------|
| — | 数据库结构检查 | ✅ PASS | documents, document_chunks, FTS 表均存在 |
| — | MCP list-tools | ✅ PASS | 7 个已启用工具正确列出 |
| — | BUG-001 模拟复现 | ✅ 已确认 | `ALTER TABLE documents` 在表不存在时抛出 `no such table` |
| — | BUG-001 修复模式验证 | ✅ 验证通过 | `KBDatabase.init()` → `_ensure_doc_columns()` 顺序正确 |
| — | BUG-002 根因定位 | ✅ 已确认 | `sys.stdout.encoding='gbk'` vs `sys.getfilesystemencoding()='utf-8'` 不匹配 |
| TC-CLI-35 | `token-budget --scope trading` | ⚠️ PASS* | 当前数据库已初始化，返回正常结果（315K 字，263K tokens） |
| TC-CLI-14 | `search --query "止损"` | ⚠️ PASS* | 返回 3 条 chunk 结果，有 extract 报错（文件不存在） |
| TC-CLI-47 | `find --query "不等回调"` | ⚠️ PASS* | 返回 1 条结果，但 JSON 中中文全部乱码 |
| — | `mcp-smoke-test` | ✅ PASS | 全部安全检查通过（路径脱敏、审计日志、未知工具拒绝、路径输入拒绝） |
| TC-CLI-58 | `agent-test` | ⚠️ PASS* | Agent 5/5 返回有效答案，但 3/5 因 GBK 无法编码 Emoji 导致 print 异常，汇总"5/5 passed"与单条"PASS=False"矛盾 |
| — | folder_analysis 报告读取 | ✅ PASS | 文件存在，UTF-8 内容可读（通过 Python 直接读） |

> \* 标注 \* 的测试在当前已初始化数据库的环境下通过，但存在编码或冷启动相关问题。

### 代码审查 Bug 清单

| Bug ID | 严重级别 | 描述 | 状态 |
|--------|----------|------|------|
| BUG-001 | P0 阻塞 | `_ensure_doc_columns()` 在 8+ 函数中缺少前置 `KBDatabase.init()`，冷启动时 `no such table: documents` | 已定位 |
| BUG-002 | P1 严重 | Windows 终端 stdout GBK 编码 vs 文件系统 UTF-8 编码不匹配，中文全部乱码 | 已定位 |
| BUG-003 | P2 | `agent_test` 中 print() 异常被误认为 Agent 工具调用失败 | 已定位 |
| BUG-004 | P3 | `search` 命令在已删除/移动文件上 extract 报 FileNotFoundError | 已定位 |

### 已验证正常的功能

1. **MCP 安全机制** — 完整通过 smoke test：
   - 路径脱敏 ✓
   - 未知工具拒绝 ✓
   - 路径参数拒绝 ✓
   - 审计日志写入 ✓
2. **FTS 全文搜索** — 基本功能正常
3. **DeepSeek LLM 调用** — Agent test 5/5 正确执行多轮工具调用
4. **Agent Runtime** — 6-step 14+ tool-calls，执行正常

### 优先修复建议

1. **立即修复 BUG-001**：创建统一 `_init_db(cfg)` 函数，在所有 workflow_mainline.py 和 bundle_builder.py 的 DB 操作前调用
2. **立即修复 BUG-002**：CLI 入口 `main.py` 添加 `sys.stdout.reconfigure(encoding='utf-8')` + 批处理中加 `chcp 65001`
3. **后续修复 BUG-003**：agent_test print 用 try/except UnicodeEncodeError 包裹
4. **后续修复 BUG-004**：search/extract 前检查文件存在性
