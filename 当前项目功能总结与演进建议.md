# kb-console：当前功能总结与演进建议

文档生成日期：2026年4月27日

## 第一部分：当前已实现的核心功能

本项目目前已经从一个简单的数据处理脚本，演进为一个**具备完整 Agent Runtime 和 MCP（Model Context Protocol）标准接口的本地智能化知识库系统**。核心模块可以分为以下几层：

### 1. 数据接入与处理层 (Ingestion & Processing)
*   **多格式文件扫描**：支持扫描指定目录下的 `.docx`, `.doc`, `.md`, `.txt` 文件。
*   **智能采样与拆块 (Chunking)**：对长文本进行前、中、后智能采样，并将长文档拆分为适合 LLM 消费的 Chunk，保证大文档在检索时不会因为长度超限丢失细节。
*   **标签归一化与清洗**：支持针对提取出的标签系统进行归一化清洗，维护标签库的一致性。

### 2. 存储与检索层 (Storage & Retrieval)
*   **SQLite + FTS5 全文检索引擎**：
    *   使用本地无依赖的 SQLite 存储文档元数据。
    *   实现 FTS5 高效的文档与文档块（Chunk）级全文检索。
    *   支持安全参数绑定，防止 SQL 注入及特殊字符查询错误。

### 3. AI 分析与智能分类层 (LLM Pipeline)
*   **DeepSeek 深度集成**：结合 DeepSeek 模型（`deepseek-v4-flash`）对抓取的内容进行理解、结构化提取与自动标签分类。
*   **规则 + LLM 混合策略**：支持预设规则（如课程材料、笔记特征过滤）结合大模型理解的双重分类。
*   **Review UI 及人工干预反馈**：生成基于 HTML 的 Review 界面，允许对机器生成的置信度存疑的结果进行人工复核，并将修改写回数据库（CSV 更新机制）。

### 4. 产出物与报表展现层 (Presentation)
*   **Dashboard 与本地 Web 服务**：基于 FastAPI 和 uvicorn 提供只读的 Web 和 API 服务，渲染 HTML Dashboard 获取知识库大盘概况。
*   **月报 / 季报生成**：可自动统计、对比并大模型归纳指定月份或季度的重点输入内容。
*   **模型上下文打包 (Context Bundle)**：能够将一段时间内的精华资料打成 Context Bundle，便于一键丢给在线大模型进行深度长文生成或思考。

### 5. 智能化交互接入层 (Agent & MCP Server)
*   **Agent Runtime**：内部构建了完善的沙盒执行环境（Tool Registry, Schema, Executor），支持多轮工具调用（Tool-Calling）以及权限校验。
*   **MCP (Model Context Protocol)**：
    *   将能力以 MCP 协议向外部环境暴露，支持 `stdio` 标准输入输出模式以及 `http` Localhost 模式（便于 Claude Desktop、Cursor 等集成）。
    *   **安全性护栏**：严格的只读控制，路径脱敏（隐藏真实物理路径），返回长度截断，未知参数及工具过滤。
    *   **完整审计日志 (Audit Log)**：每次 MCP / Agent 对工具的访问都会被记录时间、参数和是否通过。
*   **目前对外提供的智能工具 API (Tools)**：
    1.  `kb.search_documents`: 搜索文档，支持按词、按月、按分类过滤。
    2.  `kb.search_chunks`: Chunk内容粒度的精确段落全文检索。
    3.  `kb.get_document`: 获取文档详细全文。
    4.  `kb.compare_periods`: 对比多个时间段内同个主题的发展/变化。
    5.  `kb.summarize_month`: 聚合特定月份的知识提取摘要。
    6.  `kb.find_writing_candidates`: 寻找有潜力的文章/内容输入作为写作候选。
    7.  `kb.cluster_project_ideas`: 聚类并统计经常出现的项目想法和标签。

---

## 第二部分：距离“可堪一用”还需要做哪些？（演进与重构建议）

虽然系统在底层管线和接口协议上已经很完备，但要从一个“自用极客工具”变成“稳定可用、随时可用的生产力应用”，建议还需要在以下几个维度进行加强：

### 1. 增量同步与文件系统监听 (核心痛点)
*   **现状**：目前的 scan 强依赖主动触发跑批，重新全量计算或依靠比对，大规模文件时很慢。
*   **建议**：
    *   实现真正的 **增量更新 (Incremental Sync)**，记录文件的 `mtime` 或 `hash`，只处理有变动的文件。
    *   或者引入 `watchdog` 等库，实现一个后台驻留的守护进程，在用户保存 `.md` 时自动提取并入库。
    *   处理好文件**重命名、移动和删除**的级联反应（清理对应的 SQLite 和 Chunk 关联记录）。

### 2. 从 FTS5 全文检索走向 RAG (语义向量检索)
*   **现状**：依靠 SQLite 的 FTS5 只能进行“关键词精准匹配”，这会漏掉许多意思相近但用词不同的内容（例如搜“AI”，不会命中纯写着“人工智能”的文档）。
*   **建议**：
    *   引入本地或轻量级向量数据库（如 `ChromaDB`, `LanceDB`, 或者为 SQLite 编译的 `sqlite-vec` 插件）。
    *   在 Chunking 阶段，使用轻量本地 Embedding 模型（如 `bge-m3`、`m3e` 等）生成向量并存储。
    *   对外暴露的 MCP search 工具，升级为 **关键字查找 + 语义检索的混合检索 (Hybrid Search)**，大幅提升大模型的命中率。

### 3. 多模态与外部生态对接 (数据源扩充)
*   **现状**：仅支持 Word、txt、md 类静态文本。
*   **建议**：
    *   **PDF 支持**（强烈建议）：PDF 是学习资料重灾区，可集成 `pdfplumber` 或 `pymupdf`。
    *   图片内容 (OCR)：对于截图可以使用类似于 `pytesseract` 获取关键文字。
    *   主流笔记软件打通：直接接入 Obsidian / Notion API 等，而非依赖用户定期导出的 markdown。

### 4. MCP 与实际端侧体验的闭环测试
*   **现状**：MCP 服务已通，但在 Claude Desktop 等实际宿主上表现未知。
*   **建议**：
    *   真实编写配置进 `Claude Desktop` 或 `Cursor` 的配置并长期使用。
    *   在使用中观察：LLM 对你写的 tool description 理解够不够准确？返回的 json 够不够精简？（往往初期结果过大，大模型读不过来报错）。及时根据实测调整 `max_result_chars` 和 prompt。

### 5. 一键打包与交互式入口 (UX)
*   **现状**：用户要在命令行中输入 `python main.py <command>`，需要维护 `.venv`。
*   **建议**：
    *   使用 `PyInstaller` 或者类似工具构建独立 `.exe`。
    *   构建一个简单的常驻系统托盘应用 (System Tray)，提供“立即同步”、“查看大盘 Dashboard”、“停止服务”等一键操作，隐藏掉复杂的脚本执行流程，这就真正是一个“开箱即用”的产品了。