# kb-console — 项目情况总结

> 生成日期：2026-05-03

## 项目定位

一个 **Word-first 本地知识库系统**，核心功能是从桌面文档（尤其是 `.docx`）中扫描、提取、分类、存储和检索个人知识资产。具备完整的 CLI 工具链、Streamlit GUI、以及 MCP (Model Context Protocol) 服务接口，可被 Claude Desktop / Cursor 等 AI 工具集成调用。

## 项目结构

```
kb-console/
├── streamlit_app.py          # Streamlit GUI 主入口 (KB Console)
├── ui_helpers.py             # GUI 辅助函数（路径、剪贴板、Markdown 预览等）
├── run_kb_console.bat        # 一键启动 GUI 的批处理
├── run_weekly_organize.bat   # 每周整理批处理
├── run_trading_monthly_report.bat  # 交易月报批处理
├── deepseek_prompt.txt       # DeepSeek LLM 系统提示词模板
├── windows_task_scheduler.md # Windows 计划任务配置说明
├── 当前项目功能总结与演进建议.md  # 之前的功能总结文档
├── docs/                     # 知识库原始文档（按分类/月份组织）
│   ├── AI与工具化/
│   ├── 交易复盘/
│   ├── 交易系统与方法论/
│   ├── 交易记录/
│   ├── 个人随笔与自我观察/
│   ├── 外部资料与待排除内容/
│   ├── 无法判断/
│   ├── 项目想法与反复出现的问题/
│   └── _weekly_inbox/        # 每周收件箱（待整理）
├── kb_tool/                  # ★ 核心后端
│   ├── main.py               # CLI 入口 (argparse)
│   ├── config.yaml           # 全局配置
│   ├── scanner.py            # 文件扫描
│   ├── extractor.py          # 文本提取（docx/txt/md → 纯文本）
│   ├── sampler.py            # 智能采样（前/中/后/随机/关键词上下文）
│   ├── chunker.py            # 长文本分块
│   ├── classifier_rules.py   # 规则分类器
│   ├── tag_normalizer.py     # 标签归一化
│   ├── llm_classifier.py     # LLM 分类器（DeepSeek）
│   ├── database.py           # SQLite + FTS5 全文检索引擎
│   ├── bundle_builder.py     # Context Bundle 打包器
│   ├── folder_analyzer.py    # 文件夹分析
│   ├── project_analyzer.py   # 项目分析
│   ├── profile_analyzer.py   # 个人画像分析
│   ├── monthly_report.py     # 月报生成
│   ├── quarterly_report.py   # 季报生成
│   ├── docs_migrator.py      # 文档迁移工具
│   ├── docs_stats.py         # 文档统计
│   ├── review_ui.py          # HTML Review 界面（人工复核）
│   ├── server.py             # FastAPI Web 服务
│   ├── dashboard.py          # Dashboard 大盘
│   ├── workflow_mainline.py  # 工作流编排（核心管线）
│   ├── agent/                # Agent Runtime（沙盒执行环境）
│   │   ├── runtime.py
│   │   ├── tool_registry.py
│   │   ├── tool_executor.py
│   │   ├── tool_schemas.py
│   │   ├── permissions.py
│   │   ├── audit.py
│   │   └── prompts.py
│   ├── mcp_server/           # MCP 协议服务端
│   │   ├── server.py
│   │   ├── tools.py
│   │   ├── adapters.py
│   │   ├── resources.py
│   │   ├── prompts.py
│   │   ├── audit.py
│   │   └── auth.py
│   ├── llm_providers/        # LLM 提供商适配
│   │   ├── deepseek_provider.py  # DeepSeek（主力）
│   │   ├── claude_provider.py
│   │   ├── gemini_provider.py
│   │   ├── openai_provider.py
│   │   └── base.py
│   └── kb_out/               # 所有输出产物
│       ├── kb.sqlite3        # SQLite 数据库
│       ├── reports/          # 报告输出
│       ├── bundles/          # Context Bundle 输出
│       ├── logs/             # 运行日志 / LLM 调用日志 / 审计日志
│       └── dashboard/        # HTML Dashboard
└── .venv/                    # Python 虚拟环境
```

## 技术栈

| 组件 | 技术选型 |
|------|----------|
| 语言 | Python 3.12+ |
| 存储 | SQLite + FTS5 全文检索 |
| LLM | DeepSeek v4-flash（主力），同时支持 Claude / Gemini / OpenAI |
| GUI | Streamlit |
| Web 服务 | FastAPI + uvicorn |
| Agent | 自建 Tool Registry + Sandbox Executor |
| MCP | Model Context Protocol（stdio + HTTP 双模式） |

## 核心工作流

### 1. 每周整理 (Weekly Organize)
扫描 `source_dirs` → 提取文本 → 智能采样 → 规则+LLM 分类 → 标签归一化 → 写入 `docs/` + SQLite → 生成周报

### 2. 交易分析 (Trading)
- 构建 Trading Bundle → LLM 月报 / 系统构建 / 主题分析
- 分析 `交易系统与方法论`、`交易心理与情绪`、`交易复盘`、`交易记录` 四个分类

### 3. 项目/文件夹全文分析
对指定文件夹进行全文打包（Bundle），支持 LLM 深度问答分析

### 4. 个人画像 (Profile)
基于全部 / trading / ai-projects 范围的知识输入，生成个人认知画像报告

### 5. MCP 对外服务
通过 stdio 或 HTTP 暴露 7 个工具：
- `kb.search_documents` — 文档级全文搜索
- `kb.search_chunks` — Chunk 级段落检索
- `kb.get_document` — 获取文档全文
- `kb.compare_periods` — 跨时段主题对比
- `kb.summarize_month` — 月度知识摘要
- `kb.find_writing_candidates` — 写作候选推荐
- `kb.cluster_project_ideas` — 项目想法聚类

## 安全策略

- 源文件只读，不移动/删除/改名
- 输出仅写入 `kb_out/{reports,bundles,logs}`
- MCP 路径脱敏（隐藏真实物理路径）
- 完整审计日志（Agent + MCP 双向记录）

## 已知局限与演进方向

1. **增量同步** — 目前依赖主动全量扫描，缺少文件系统监听（watchdog）
2. **语义检索** — 目前仅 FTS5 关键词匹配，缺少向量嵌入（ChromaDB / sqlite-vec）
3. **多模态** — 不支持 PDF 和图片 OCR
4. **打包分发** — 目前依赖 Python 环境运行，未打包为独立 exe
5. **知识去重/冲突解决** — 暂未实现

## 配置要点

- LLM 默认使用 **DeepSeek v4-flash**，API Key 通过环境变量 `DEEPSEEK_API_KEY` 注入
- 源文件扫描范围为 `~/Desktop`（包括桌面所有文件）
- 知识库核心文档存放于 `docs/`，按分类名/月份 两级目录组织
